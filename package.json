{
  "name": "fablab-leprototype.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tailwind:process": "npx postcss src/css/style.css --o assets/css/global.css",
    "build": "npm run tailwind:process && eleventy",
    "serve": "npm run tailwind:process && eleventy --serve",
    "clean": "rimraf docs && rm -f assets/css/global.css"
  },
  "keywords": [],
  "author": "Pierre Mauduit",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "autoprefixer": "^10.2.6",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.2.2"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
